代码：scapegoat_tree.ScapegoatTree
算法概述：一种平衡二叉树


核心思想：
1.Alpha(double)平衡因数，该因数作为衡量一棵字数是否平衡的标准。Alpha取值于（0.5，1），越大越不平衡。
2.将不平衡的子树用中序遍历拍扁，再把中点提起来，重构为新子树。

算法步骤：
1.增加节点时，顺便检查路径上的子树是否平衡。
2.如果节点的左子树size或右子树size占总size的比例超过alpha，则判定为不平衡，将该节点拍扁重构。
3.重构完成后继续执行增加节点操作。